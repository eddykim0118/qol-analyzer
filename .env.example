# API Keys for qol-analyzer
# Copy this file to .env and fill in your actual API keys

# U.S. Census Bureau API Key
# Register at: https://api.census.gov/data/key_signup.html
# Required for fetching ACS (American Community Survey) data
CENSUS_API_KEY=your_census_api_key_here

# Bureau of Labor Statistics (BLS) API Key (optional but recommended)
# Register at: https://www.bls.gov/developers/home.htm
# Provides higher rate limits for CPI data fetching
# If not provided, uses public API with lower rate limits
BLS_API_KEY=your_bls_api_key_here

# Notes:
# - Without CENSUS_API_KEY, the pipeline will fail
# - Without BLS_API_KEY, you can still fetch CPI data but with rate limits
# - Never commit your actual .env file to version control
# - API keys are free to obtain and typically issued instantly
